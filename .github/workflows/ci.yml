name: ci

# The local gates, run on every PR and on master (roadmap NU-4). Meant to
# be required checks in the master ruleset next to `gitleaks`; the job
# `name:`s below are the check names the ruleset matches on, so rename
# them only together with the ruleset. docs/runbooks/merge-gates.md
# has which checks are required and the emergency bypass.
#
# No secrets here: every value below is a dummy for a throwaway service
# or build, and the workflow token is read-only.

on:
  pull_request:
  push:
    branches: [master]

permissions:
  contents: read

concurrency:
  # One run per PR (or per master push); a new push to a PR cancels the
  # run for the commit it replaced. Master runs are never cancelled.
  group: ci-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  bot:
    name: bot
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: bot
    env:
      # Derandomised hypothesis for a merge gate: the same examples on
      # every run (tests/conftest.py registers the profile).
      HYPOTHESIS_PROFILE: ci
    steps:
      # SHA-pinned like secret-scan.yml; Dependabot bumps them monthly.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      # CI runs the uv the bot image pins, read from bot/Dockerfile so a
      # Dependabot bump of that line moves CI with it.
      - name: uv version from bot/Dockerfile
        id: uv
        run: |
          v=$(sed -n 's|^FROM ghcr.io/astral-sh/uv:\([0-9][0-9.]*\)[@ ].*|\1|p' Dockerfile)
          test -n "$v" || { echo "no pinned uv image in bot/Dockerfile" >&2; exit 1; }
          echo "version=$v" >> "$GITHUB_OUTPUT"

      - uses: astral-sh/setup-uv@c18668ad3cf93ea998bef934396af7bb5c839dc7  # v10.2.0
        with:
          version: ${{ steps.uv.outputs.version }}
          working-directory: bot
          enable-cache: true
          # Relative to working-directory, so not bot/uv.lock. The
          # suffix keeps this job's dev-group cache apart from the
          # migrations job's runtime-only one.
          cache-dependency-glob: uv.lock
          cache-suffix: bot

      # --locked, not --frozen: fail when uv.lock no longer matches
      # pyproject.toml. --frozen installs the lock without that check,
      # and a plain `uv run` after it re-locks on the runner, so a PR
      # that forgot `uv lock` went green while the image — built from
      # the stale lock — lacked the new dependency.
      - name: Install (lockfile must match pyproject.toml)
        run: uv sync --locked

      # --no-sync: test exactly the environment the lock installed.
      - name: pytest
        run: uv run --no-sync pytest -q -n auto

  dashboard:
    name: dashboard
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: dashboard
    env:
      NEXT_TELEMETRY_DISABLED: "1"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          # Same major as dashboard/Dockerfile's node:22-alpine.
          node-version: "22"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - run: npm ci
      - name: Typecheck
        run: npx tsc --noEmit
      - name: Lint
        run: npm run lint
      - name: Unit tests
        run: npm test
      - name: Production build
        run: npm run build
        env:
          # Dummies. The build does not need a live database or Redis;
          # these only keep a module-scope read from falling back to
          # something that looks like a real deployment.
          DATABASE_URL: postgresql://ci:ci@127.0.0.1:5432/ci
          REDIS_URL: redis://127.0.0.1:6379/0

  # `npm run test:integration`: real Postgres via testcontainers (the
  # runner has Docker). The only tests that exercise the
  # reserveBotStart row lock and the per-tenant Postgres roles; the unit
  # suite mocks both. A separate job so it can be made a required
  # check on its own once it has a green record.
  dashboard-integration:
    name: dashboard-integration
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - run: npm ci
      - name: Integration tests (testcontainers Postgres)
        run: npm run test:integration

  migrations:
    name: migrations
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: bot
    services:
      postgres:
        # Same image as the postgres service in docker-compose.yml.
        image: postgres:16-alpine
        env:
          POSTGRES_DB: hypertrade
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d hypertrade"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/hypertrade
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      # CI runs the uv the bot image pins, read from bot/Dockerfile so a
      # Dependabot bump of that line moves CI with it.
      - name: uv version from bot/Dockerfile
        id: uv
        run: |
          v=$(sed -n 's|^FROM ghcr.io/astral-sh/uv:\([0-9][0-9.]*\)[@ ].*|\1|p' Dockerfile)
          test -n "$v" || { echo "no pinned uv image in bot/Dockerfile" >&2; exit 1; }
          echo "version=$v" >> "$GITHUB_OUTPUT"

      - uses: astral-sh/setup-uv@c18668ad3cf93ea998bef934396af7bb5c839dc7  # v10.2.0
        with:
          version: ${{ steps.uv.outputs.version }}
          working-directory: bot
          enable-cache: true
          cache-dependency-glob: uv.lock  # relative to working-directory
          cache-suffix: migrations

      # --locked and --no-sync as in the bot job.
      - name: Install (lockfile must match, runtime deps only)
        run: uv sync --locked --no-dev

      # Every migration from 0001 up, on an empty database. Catches a
      # migration that only works against production's existing rows —
      # 0011 did until it stopped requiring the operator tenant row
      # when there is nothing to backfill.
      - name: alembic upgrade head (empty database)
        run: uv run --no-sync alembic upgrade head

      - name: Assert the database is at head
        run: |
          current=$(uv run --no-sync alembic current 2>/dev/null)
          heads=$(uv run --no-sync alembic heads 2>/dev/null)
          echo "current: $current"
          echo "heads:   $heads"
          test "$(echo "$heads" | wc -l)" -eq 1
          test "$current" = "$heads"
